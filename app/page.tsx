export default function Home() {
  return (
    <main style={{ minHeight: '100vh', padding: '48px 24px' }}>
      <section style={{ maxWidth: 1100, margin: '0 auto' }}>
        <p style={{ margin: 0, fontSize: 14, fontWeight: 700, letterSpacing: 1 }}>LAND BD</p>
        <h1 style={{ margin: '12px 0', fontSize: 'clamp(2rem, 6vw, 4rem)', lineHeight: 1.05 }}>
          বাংলাদেশের ভূমি তথ্য ও হিসাব প্ল্যাটফর্ম
        </h1>
        <p style={{ maxWidth: 720, fontSize: 18, lineHeight: 1.7, color: '#59636e' }}>
          Next.js App Router foundation is installed. Existing HTML prototypes remain in the repository while the production application is migrated incrementally.
        </p>
      </section>
    </main>
  );
}
